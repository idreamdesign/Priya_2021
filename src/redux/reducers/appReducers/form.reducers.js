import * as Types from '../../root.types';

// Form Reducers (Dietitian, Notification, Physiotherapist, etc)

const initialState = {};

const formReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default formReducer;